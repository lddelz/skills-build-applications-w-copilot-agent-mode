# OctoFit Tracker Frontend

Set the Vite environment variable VITE_CODESPACE_NAME in .env.local when running in GitHub Codespaces.

Example:

```bash
VITE_CODESPACE_NAME=my-codespace
```

When VITE_CODESPACE_NAME is unset, the app falls back to http://localhost:8000 for API requests.
